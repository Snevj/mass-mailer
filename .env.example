# Copy this file to .env.local and fill in real values.
# .env.local is gitignored; this file is NOT — never paste real secrets here.

# ---- Supabase (Settings → API) ----
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGci...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...

# ---- Gmail SMTP fallback ----
# Only used if a campaign doesn't specify a sender. Generate an app password
# at https://myaccount.google.com/apppasswords (needs 2FA enabled).
GMAIL_ADDRESS=you@gmail.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx
GMAIL_FROM_NAME=Your Name

# ---- Web UI login ----
APP_PASSWORD=change-me

# ---- Session cookie secret (32+ chars) ----
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me-to-a-random-32-plus-char-string

# ---- Sender-credential encryption key (16+ chars, 32+ recommended) ----
# Encrypts senders.app_password at rest (src/lib/crypto.ts). Kept separate
# from SESSION_SECRET so rotating one doesn't break the other. Optional —
# falls back to SESSION_SECRET if unset, but set this explicitly for new
# installs. Generate the same way as SESSION_SECRET.
ENCRYPTION_KEY=change-me-to-a-different-random-32-plus-char-string

# ---- Cron authentication ----
# Generate the same way. Used by the Supabase pg_cron → /api/tick caller.
CRON_SECRET=change-me-to-another-random-string

# ---- Public URL (production) ----
# Used for tracking pixels, click-tracked links, and unsubscribe URLs.
# Set to your deployed URL (https://yourapp.vercel.app) in production.
APP_URL=http://localhost:3000

# ---- AI replies (optional) ----
# Powers the "Generate AI reply" button in the Replies inbox. OpenAI-compatible.
#   OpenAI: AI_BASE_URL=https://api.openai.com/v1       AI_MODEL=gpt-4o-mini
#   Groq:   AI_BASE_URL=https://api.groq.com/openai/v1  AI_MODEL=llama-3.3-70b-versatile
AI_API_KEY=
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
